name: Java code deployment
on:
  push:
    branches:
            - DEV     
  workflow_dispatch:
env:
    deploy_server: QA
    war_filename: sample-project-0.0.1-SNAPSHOT.war
    #   git_sha: ${{ GITHUB_SHA }}
jobs:
   clone:
      runs-on: self-hosted
      steps:
       - uses: actions/checkout@v3
